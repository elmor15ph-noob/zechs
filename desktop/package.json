{
  "name": "brain-app-desktop",
  "version": "1.0.0",
  "description": "Brain App - Desktop Application",
  "main": "dist/main.js",
  "homepage": "./",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-builder": "^24.6.4",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "dev": "concurrently \"tsc --watch\" \"wait-on dist/main.js && electron .\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
